import {
	LoginActions,
	LoginGetters,
	LoginMutations,
} from "@/login/models/login.emums";

export const LOGIN_STORE = {
	GETTERS: LoginGetters,
	MUTATIONS: LoginMutations,
	ACTIONS: LoginActions,
};
